//let class = "Hello World";
//class is a reserved keyword and cannot be used as an identifier. This will result in a syntax error..

//let return = 5;
//return is also a reserved keyword and cannot be used as an identifier. This will also result in a syntax error.

//let myVariable = 10;
//This is a valid identifier and can be used to store a value.

//they look like reserved keywords but they are not. They are just identifiers that can be used to store values. However, it is not recommended to use them as identifiers because it can lead to confusion and make the code harder to read.
//let undefined = "This will work but not good practice";
//let infinity = "This will also work but not good practice";